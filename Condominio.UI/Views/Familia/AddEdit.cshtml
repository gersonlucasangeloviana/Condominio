@model GestaoGestaoCondominio.Domain.Entities.Familia

@{
    ViewData["Title"] = "AddEdit";
}

<h1>@(Model == null ? "Adicionar" : "Editar") Família</h1>
<hr />
<div class="row">
    <div class="col-md-4">
        <form asp-action="AddEdit" method="post">
            <div asp-validation-summary="ModelOnly" class="text-danger"></div>
            <div class="form-group" hidden>
                <label asp-for="FamiliaId" class="control-label"></label>
                <input asp-for="FamiliaId" class="form-control" />
                <span asp-validation-for="FamiliaId" class="text-danger"></span>
            </div>
            <div class="form-group">
                <label asp-for="Nome" class="control-label"></label>
                <input asp-for="Nome" class="form-control" />
                <span asp-validation-for="Nome" class="text-danger"></span>
            </div>
            <div class="form-group">
                <label asp-for="CondominioId" class="control-label"></label>
                <select class="form-control" asp-for="CondominioId" asp-items="ViewBag.Condominios">
                    <option value="">Selecione ...</option>
                </select>
                <span asp-validation-for="CondominioId" class="text-danger"></span>
            </div>
            <div class="form-group">
                <label asp-for="Apartamento" class="control-label"></label>
                <input asp-for="Apartamento" class="form-control" />
                <span asp-validation-for="Apartamento" class="text-danger"></span>
            </div>
            <div class="form-group">
                <input type="submit" value="Enviar" class="btn btn-primary" />
            </div>
        </form>
    </div>
</div>

@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
}
